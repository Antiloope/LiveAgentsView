# Overrides de desarrollo. Se combina con compose.yaml:
#   docker compose -f compose.yaml -f compose.dev.yaml ...

services:
  db:
  # Agregar acá apps con target dev + watch cuando existan en apps/
