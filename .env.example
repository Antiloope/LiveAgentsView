# Copy this file to `.env` for local development if you want to override the
# default port. Not required otherwise — compose.yaml already defaults it.

# Port the daemon's dashboard/API is published on (host side).
LAV_PORT=8420
