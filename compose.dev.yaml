# Development overrides. Combined with compose.yaml:
#   docker compose -f compose.yaml -f compose.dev.yaml ...
#
# Nothing dev-only is needed right now — kept as the file future dev-only
# tweaks (extra mounts, env) go in, and so dev-up.sh/dev-down.sh/lav-status.sh
# keep passing a consistent set of -f flags to docker compose.

services:
  lav: {}
