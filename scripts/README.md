# scripts/

Everything runnable against the project will live here. No long Docker commands
typed from memory, and no dependencies installed on the host beyond Docker.

This directory is a placeholder for now. Scripts will be added when the first
deployable and local workflow are defined.
